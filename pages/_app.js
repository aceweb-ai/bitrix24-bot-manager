// Опциональный файл _app.js для кастомизации страниц Next.js
// Можно использовать для глобальных стилей, провайдеров состояния и т.д.

export default function App({ Component, pageProps }) {
  return <Component {...pageProps} />;
}
